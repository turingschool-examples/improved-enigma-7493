<h1><%= @dish.name %></h1>
<p>Description: <%= @dish.description %></p>
<p>Chef: <%= @dish.chef.name %></p>


<h2>Ingredients</h2>
<% @dish.ingredients.each do |ingredient| %>
  <div id=<%= "ingredient-#{ingredient.id}" %>>
    <%= ingredient.name %>
    <%= button_to "Delete", dish_ingredient_path(@dish, ingredient), method: :delete %>
  </div>
<% end %>

<h2>Calories</h2>
<p>Total Calories: <%= @dish.total_calories %></p>

<%= form_with url: dish_ingredients_path(@dish), method: :post, data: { turbo: false } do |f| %>
  <%= f.label :ingredient %>
  <%= f.text_field :ingredient %>

  <%= f.submit "Submit" %>
<% end %>